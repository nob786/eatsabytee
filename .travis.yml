language: node_js

services:
  - docker

before_install:
  - docker build -t nob786/eatsabytee ./microservices/auth-service

after_success:
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin

  - docker push nob786/eatsabytee
